{
  "$jsonSchema": {
    "bsonType": "object",
    "required": ["name"],
    "properties": {
      "name": {
        "bsonType": "string",
        "label": "Name",
        "description": "Name of the nations"
      },
      "map_color": {
        "bsonType": ["string", "null"],
        "label": "Map Color",
        "description": "Hex color code for this nation's territory on the map (e.g., #FF0000)"
      },
      "territory_tiles": {
        "bsonType": "array",
        "label": "Territory Tiles",
        "description": "Array of map tile IDs that this nation controls",
        "items": {
          "bsonType": "string"
        }
      }
    }
  }
}
